# 自适应RAG系统依赖文件
# 运行以下命令安装: pip install -r requirements.txt

# 核心框架
langchain==0.3.27
langchain-core>=0.3.50,<0.4
langgraph==0.3.34
langchain-community==0.3.15
langchain-text-splitters==0.3.11
langchain-huggingface==0.1.2  # 新的HuggingFaceEmbeddings实现，避免废弃警告

# LLM集成
langchain-ollama==0.2.2  # 已切换到 tongyi 后端，无需此依赖
langchain-openai==0.2.1

# 向量数据库和嵌入
pymilvus[milvus_lite]==2.6.5
langchain-milvus==0.1.10
sentence-transformers==2.6.1
transformers==4.46.3
huggingface-hub==0.26.0  # HuggingFace Hub客户端

# 文档处理
pypdf==5.1.0  # PDF文件解析
PyMuPDF==1.25.3  # PDF文本提取（比pypdf质量更高，支持数学公式区域识别）
python-docx==1.1.2  # Word (.docx) 文件解析 + OMML数学公式提取
python-pptx==1.0.2  # PowerPoint (.pptx) 文件解析 + OMML数学公式提取
docx2txt==0.8  # Word (.docx) 简单文本提取（回退方案）
lxml==5.3.0  # XML解析，用于OMML数学公式转LaTeX
openpyxl==3.1.5  # Excel (.xlsx) 文件解析
ebooklib==0.18  # EPUB 电子书解析
beautifulsoup4==4.12.2
requests==2.32.3
Pillow==10.4.0  # 图像处理，支持多模态功能
openai>=1.0.0  # VLM API 客户端（通义千问/Qwen-VL 等视觉语言模型）

# OCR 引擎（三选一，按优先级自动回退）
paddleocr>=2.7.0  # PaddleOCR（推荐，中文识别效果最好）
paddlepaddle>=2.5.0  # PaddleOCR 依赖的深度学习框架
easyocr>=1.7.0  # EasyOCR（回退方案，多语言支持）
pytesseract>=0.3.10  # Tesseract OCR（最后回退方案，需系统安装 tesseract-ocr）
rank-bm25==0.2.2  # BM25检索，用于混合搜索
elasticsearch==8.9.0  # Elasticsearch 客户端，用于大规模 BM25 检索

# 幻觉检测
scikit-learn==1.3.0  # 相似度计算

# 网络搜索
tavily-python==0.7.17
langchain-tavily==0.2.15  # 新的Tavily集成，避免弃用警告

# 数据处理
numpy==1.26.4  # 兼容jax，避免NumPy 2.x兼容性问题
pandas==2.0.3

# 工具库
python-dotenv==1.0.1
# mcp==1.28.1 要求 pydantic>=2.11.0；langchain==0.3.27 要求 >=2.7.4,<3.0.0；langchain-ollama 要求 >=2.9.0。
# 取交集锁定 2.11.7（原 2.9.2 与 mcp 冲突导致 ResolutionImpossible）
pydantic==2.11.7
# langchain-mcp-adapters 0.2.0 要求 typing-extensions>=4.14.0（原 4.12.2 与之冲突）
typing-extensions>=4.14.0

# GraphRAG相关
networkx==3.2.1  # 图结构处理
python-louvain==0.16  # 社区检测

# 缓存和性能优化
diskcache==5.6.0
redis[hiredis]==5.2.1  # 会话持久化存储（hiredis 加速序列化）
# Agent 状态持久化（Checkpointer）。已用 --dry-run 验证: 0.2.x 与 langgraph==0.3.34 冲突，锁定 0.1.2
langgraph-checkpoint-redis==0.1.2

# MCP (Model Context Protocol) 工具协议
# 注意: 0.2.x 全系列引入 langchain_core.messages.content 导入，需要 langchain-core>=1.0.0
# （0.2.0 未声明依赖导致 pip 解析通过但运行时报 ModuleNotFoundError；0.2.1+ 显式要求 >=1.0.0）
# 本项目 langchain-core 为 0.3.x，故锁 0.1.14（最后兼容版本，含 MultiServerMCPClient + get_tools(server_name=) API）
mcp==1.28.1
langchain-mcp-adapters==0.1.14
playwright==1.49.0  # 行动型工具-网页抓取（可选；slim 镜像可跳过，工具自动降级）

# 异步HTTP请求（用于并发处理）
aiohttp==3.11.14

# 系统监控
psutil==5.9.0

# 可视化（可选，retrieval_evaluation 绘图用）
contourpy==1.1.1  # matplotlib 的依赖，明确版本避免解析慢
matplotlib==3.8.0
seaborn==0.13.0

# Web服务器
fastapi==0.115.6
uvicorn==0.34.0
python-multipart==0.0.20

# PyYAML（Prompt 配置化）
PyYAML>=6.0.1

# Kaggle/Notebook 兼容（支持嵌套事件循环）
nest_asyncio>=1.5.0

# LangSmith：langchain-core 0.3.72+ 要求 >=0.3.45；langchain-community 0.3.x 要求 <0.4
langsmith>=0.3.45,<0.4
